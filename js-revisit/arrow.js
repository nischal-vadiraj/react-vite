const getFive = () => 5
const addFive = a => a + 5
const divide = (a, b) => a / b

// same thing 
function getFive() {
  return 5
}
function addFive(a) {
  return a + 5
}
function divide(a, b) {
  return a / b
}

